<h1>Questions</h1>

  <ul>
    <% @questions.each do |question| %>
      <li><%= link_to question.title, question_path(question) %></li>
    <%end%>
  </ul>

<%=link_to "Ask A Question", new_question_path(@question) %>
